# Dashboard de Recompra — Plano de Implementação

> **Para workers agênticos:** SUB-SKILL OBRIGATÓRIA: use superpowers:subagent-driven-development (recomendado) ou superpowers:executing-plans para implementar tarefa a tarefa. Os passos usam checkbox (`- [ ]`).

**Goal:** Adicionar o dashboard "Recompra" — para cada cliente, mostra se ele está dentro ou fora do **próprio ritmo de compra** (mediana dos intervalos), classificado em 🟢 em dia / 🟡 atrasando / 🔴 atrasado / ⚪ sem padrão, priorizando quem está mais fora.

**Arquitetura:** Novo módulo `app/recompra_service.py` com a matemática em **função pura testável** (`classificar_recompra`) + um agregador de banco (`montar_recompra`). Uma rota `GET /dashboard/recompra` em `routers/dashboard.py` (irmã de `/dashboard/analise`), com cache por recorte, renderizando KPIs + tabela. Reusa o padrão de `analise_service.py`/`dashboard.py`.

**Tech Stack:** Python 3.11, FastAPI, SQLAlchemy Core (`text`), PostgreSQL (`array_agg`), Jinja2, HTMX, pytest.

**Spec:** `docs/superpowers/specs/2026-06-11-recompra-design.md`

---

## Estrutura de arquivos

| Arquivo | Responsabilidade | Ação |
|---------|------------------|------|
| `app/recompra_service.py` | Funções puras (`_mediana`, `_percentil`, `classificar_recompra`) + DB (`montar_recompra`, `opcoes_recompra`) | Criar |
| `app/tests/test_recompra_service.py` | Testes do service (puro + DB) | Criar |
| `app/routers/dashboard.py` | Rota `GET /dashboard/recompra` + cache por recorte | Modificar |
| `app/templates/recompra.html` | Página (filtros + parcial) | Criar |
| `app/templates/partials/recompra_paineis.html` | KPIs + tabela (parte trocada por HTMX) | Criar |
| `app/templates/dashboards.html` | Card "Recompra" no hub | Modificar |

**Comando de teste (sempre):** a partir da raiz `C:\Users\dbarbosa\Documents\PROJETOS\SBR_LEEDS`:
```
docker compose exec -T app sh -c "cd /code && PYTHONPATH=/code pytest -q"
```
Escopo: `... pytest tests/test_recompra_service.py -q`. NÃO rode pytest direto no host (o DB só existe no container e há um `__init__.py` em `/code` que exige `PYTHONPATH=/code`). Edições no host em `app/` refletem em `/code` automaticamente.

---

## Task 1: Funções puras — mediana, percentil, classificação

**Files:**
- Create: `app/recompra_service.py`
- Create: `app/tests/test_recompra_service.py`

- [ ] **Step 1: Escrever os testes das funções puras**

Criar `app/tests/test_recompra_service.py`:

```python
from datetime import date, timedelta

import recompra_service as svc


def _datas(inicio: date, *intervalos: int) -> list[date]:
    """Constrói datas de compra a partir de um início e dos intervalos entre elas."""
    datas = [inicio]
    for d in intervalos:
        datas.append(datas[-1] + timedelta(days=d))
    return datas


# ---- mediana ----

def test_mediana_impar():
    assert svc._mediana([10, 30, 20]) == 20

def test_mediana_par_media_dos_centrais():
    assert svc._mediana([10, 20, 30, 40]) == 25

def test_mediana_vazia_zero():
    assert svc._mediana([]) == 0.0


# ---- percentil (interpolação linear) ----

def test_percentil_p90_interpola():
    # [20,30,40], p=0.9 -> rank=1.8 -> 30 + 0.8*(40-30) = 38
    assert svc._percentil([20, 30, 40], 0.9) == 38.0

def test_percentil_um_elemento():
    assert svc._percentil([25], 0.9) == 25.0

def test_percentil_vazio_zero():
    assert svc._percentil([], 0.9) == 0.0


# ---- classificar_recompra ----

def test_sem_padrao_com_menos_de_3_compras():
    for ints in ([], [30]):  # 1 e 2 compras
        datas = _datas(date(2026, 1, 1), *ints)
        r = svc.classificar_recompra(datas, date(2026, 3, 1), receita_total=200)
        assert r["faixa"] == "sem_padrao"
        assert r["indice"] is None
        assert r["mediana"] is None
        assert r["n_compras"] == len(datas)
        assert r["ultima_compra"] == datas[-1]
        # ticket médio ainda é calculado
        assert r["ticket_medio"] == round(200 / len(datas), 2)


def test_faixa_em_dia_no_limite_da_mediana():
    # intervalos [20,30,40] -> mediana 30, p90 38. última = inicio+90.
    datas = _datas(date(2026, 1, 1), 20, 30, 40)
    ultima = datas[-1]
    r = svc.classificar_recompra(datas, ultima + timedelta(days=30), receita_total=400)
    assert r["mediana"] == 30.0
    assert r["maior_intervalo_normal"] == 38.0
    assert r["dias_sem_comprar"] == 30
    assert r["faixa"] == "em_dia"        # 30 <= mediana(30)
    assert r["indice"] == 1.0


def test_faixa_atrasando_entre_mediana_e_p90():
    datas = _datas(date(2026, 1, 1), 20, 30, 40)
    ultima = datas[-1]
    r = svc.classificar_recompra(datas, ultima + timedelta(days=35), receita_total=400)
    assert r["faixa"] == "atrasando"     # 30 < 35 <= 38


def test_faixa_atrasando_no_limite_p90():
    datas = _datas(date(2026, 1, 1), 20, 30, 40)
    ultima = datas[-1]
    r = svc.classificar_recompra(datas, ultima + timedelta(days=38), receita_total=400)
    assert r["faixa"] == "atrasando"     # 38 <= p90(38)


def test_faixa_atrasado_acima_do_p90():
    datas = _datas(date(2026, 1, 1), 20, 30, 40)
    ultima = datas[-1]
    r = svc.classificar_recompra(datas, ultima + timedelta(days=39), receita_total=400)
    assert r["faixa"] == "atrasado"      # 39 > p90(38)
    assert r["indice"] == round(39 / 30, 2)


def test_p90_robusto_a_outlier():
    # uma demora excepcional (200) não deve esticar a faixa atrasando até 200.
    datas = _datas(date(2026, 1, 1), 30, 30, 30, 200)
    ultima = datas[-1]
    r = svc.classificar_recompra(datas, ultima + timedelta(days=60), receita_total=500)
    # mediana dos intervalos [30,30,30,200] = 30; p90 < 200 (longe do outlier)
    assert r["mediana"] == 30.0
    assert r["maior_intervalo_normal"] < 200
    # 60 dias está acima da mediana e (provavelmente) do p90 -> atrasado/atrasando, nunca em dia
    assert r["faixa"] in ("atrasando", "atrasado")


def test_ticket_medio():
    datas = _datas(date(2026, 1, 1), 30, 30)  # 3 compras
    r = svc.classificar_recompra(datas, datas[-1] + timedelta(days=10), receita_total=900)
    assert r["ticket_medio"] == 300.0


def test_mediana_zero_nao_quebra():
    # três compras no mesmo dia -> intervalos [0,0], mediana 0; índice não divide por zero.
    d = date(2026, 1, 1)
    r = svc.classificar_recompra([d, d, d], d + timedelta(days=5), receita_total=300)
    assert r["mediana"] == 0.0
    assert r["indice"] == 5.0            # usa mediana mínima de 1 para o índice
    assert r["faixa"] == "atrasado"
```

- [ ] **Step 2: Rodar os testes para vê-los falhar**

Run: `docker compose exec -T app sh -c "cd /code && PYTHONPATH=/code pytest tests/test_recompra_service.py -q"`
Expected: FAIL com `ModuleNotFoundError: No module named 'recompra_service'`.

- [ ] **Step 3: Criar `app/recompra_service.py` com as funções puras**

```python
"""Serviço do dashboard de Recompra.

A matemática (datas de compra + hoje -> ritmo e faixa) é função pura, testável
sem banco — no espírito de analise_service. Os helpers de banco ficam abaixo.

Cada cliente é julgado pela régua DELE mesmo: a mediana dos intervalos entre as
compras define o ritmo; o percentil 90 dos intervalos define até onde um atraso
ainda é "normal" para ele.
"""
from datetime import date

from sqlalchemy import text
from sqlalchemy.orm import Session

FAIXA_EM_DIA = "em_dia"
FAIXA_ATRASANDO = "atrasando"
FAIXA_ATRASADO = "atrasado"
FAIXA_SEM_PADRAO = "sem_padrao"
MIN_COMPRAS = 3


def _mediana(xs: list[float]) -> float:
    s = sorted(xs)
    n = len(s)
    if n == 0:
        return 0.0
    meio = n // 2
    if n % 2:
        return float(s[meio])
    return (s[meio - 1] + s[meio]) / 2


def _percentil(xs: list[float], p: float) -> float:
    """Percentil por interpolação linear (p em [0,1]); rank = p*(n-1)."""
    s = sorted(xs)
    n = len(s)
    if n == 0:
        return 0.0
    if n == 1:
        return float(s[0])
    rank = p * (n - 1)
    lo = int(rank)
    frac = rank - lo
    if lo + 1 >= n:
        return float(s[-1])
    return s[lo] + frac * (s[lo + 1] - s[lo])


def classificar_recompra(datas: list[date], hoje: date, *, receita_total: float = 0.0) -> dict:
    """Classifica um cliente pelo próprio ritmo de compra.

    Devolve sempre: n_compras, ultima_compra, dias_sem_comprar, ticket_medio, faixa.
    Para >= MIN_COMPRAS adiciona mediana, maior_intervalo_normal e indice.
    """
    datas = sorted(datas)
    n = len(datas)
    ultima = datas[-1] if n else None
    dias_sem_comprar = (hoje - ultima).days if ultima else None
    ticket_medio = (receita_total / n) if n else 0.0

    resultado = {
        "n_compras": n,
        "ultima_compra": ultima,
        "dias_sem_comprar": dias_sem_comprar,
        "ticket_medio": round(ticket_medio, 2),
        "mediana": None,
        "maior_intervalo_normal": None,
        "indice": None,
        "faixa": FAIXA_SEM_PADRAO,
    }
    if n < MIN_COMPRAS:
        return resultado

    intervalos = [(datas[i] - datas[i - 1]).days for i in range(1, n)]
    mediana = _mediana(intervalos)
    p90 = _percentil(intervalos, 0.9)
    mediana_safe = mediana if mediana >= 1 else 1.0
    indice = dias_sem_comprar / mediana_safe

    if dias_sem_comprar <= mediana:
        faixa = FAIXA_EM_DIA
    elif dias_sem_comprar <= p90:
        faixa = FAIXA_ATRASANDO
    else:
        faixa = FAIXA_ATRASADO

    resultado.update({
        "mediana": round(mediana, 1),
        "maior_intervalo_normal": round(p90, 1),
        "indice": round(indice, 2),
        "faixa": faixa,
    })
    return resultado
```

- [ ] **Step 4: Rodar os testes para vê-los passar**

Run: `docker compose exec -T app sh -c "cd /code && PYTHONPATH=/code pytest tests/test_recompra_service.py -q"`
Expected: PASS (todos os testes das funções puras).

- [ ] **Step 5: Commit**

```bash
git add app/recompra_service.py app/tests/test_recompra_service.py
git commit -m "feat: funcoes puras de recompra (mediana, percentil, classificacao)"
```

---

## Task 2: Agregador de banco — `montar_recompra` e `opcoes_recompra`

**Files:**
- Modify: `app/recompra_service.py`
- Modify: `app/tests/test_recompra_service.py`

- [ ] **Step 1: Escrever os testes do agregador**

Acrescentar ao final de `app/tests/test_recompra_service.py`:

```python
from sqlalchemy import text


def _cli(db, doc, vendedor="Joao", uf="PI", municipio="Floriano"):
    db.execute(text("""
        INSERT INTO cliente_pedido_mobile (documento, razao_social, nome_fantasia, vendedor, inativo, municipio, uf)
        VALUES (:d, :rs, :nf, :v, FALSE, :m, :uf)
        ON CONFLICT (documento) DO UPDATE SET vendedor = EXCLUDED.vendedor
    """), {"d": doc, "rs": f"Razao {doc}", "nf": f"Farmacia {doc}", "v": vendedor, "m": municipio, "uf": uf})


_pedido_seq = [0]

def _pedido(db, doc, emissao, *, total=100.0, orcamento=False, situacao="Enviado"):
    _pedido_seq[0] += 1
    db.execute(text("""
        INSERT INTO pedido_mobile_pedido
            (pedido_numero, cliente_documento, vendedor, emissao, situacao, orcamento, total_liquido)
        VALUES (:n, :d, 'Joao', :e, :s, :o, :t)
    """), {"n": _pedido_seq[0], "d": doc, "e": emissao, "s": situacao, "o": orcamento, "t": total})


def test_montar_recompra_classifica_e_conta(db):
    # Cliente A: 4 compras regulares (~30d), última há 70d -> atrasado.
    _cli(db, "A")
    base = date(2026, 1, 1)
    for off in (0, 30, 60, 90):
        _pedido(db, "A", base + timedelta(days=off), total=200)
    hoje = base + timedelta(days=90 + 70)  # 70 dias após a última
    dados = svc.montar_recompra(db, vendedor="Joao", cidade=None, uf=None, hoje=hoje)
    a = next(c for c in dados["clientes"] if c["documento"] == "A")
    assert a["faixa"] == "atrasado"
    assert a["n_compras"] == 4
    assert dados["kpis"]["atrasado"] >= 1
    # receita dos atrasados = soma do ticket médio dos 🔴 (A: 800/4 = 200)
    assert dados["kpis"]["receita_atrasados"] >= 200


def test_montar_recompra_ignora_orcamento_e_cancelado(db):
    _cli(db, "B")
    base = date(2026, 1, 1)
    _pedido(db, "B", base, total=100)
    _pedido(db, "B", base + timedelta(days=30), total=100)
    _pedido(db, "B", base + timedelta(days=60), total=100)
    # estes NÃO contam:
    _pedido(db, "B", base + timedelta(days=70), total=100, orcamento=True)
    _pedido(db, "B", base + timedelta(days=75), total=100, situacao="Cancelado")
    dados = svc.montar_recompra(db, vendedor="Joao", cidade=None, uf=None, hoje=base + timedelta(days=80))
    b = next(c for c in dados["clientes"] if c["documento"] == "B")
    assert b["n_compras"] == 3   # só os efetivos


def test_montar_recompra_sem_padrao_vai_para_o_fim(db):
    _cli(db, "C")  # 1 compra só -> sem padrão
    _pedido(db, "C", date(2026, 1, 1), total=50)
    _cli(db, "D")  # 3 compras, atrasado
    base = date(2026, 1, 1)
    for off in (0, 30, 60):
        _pedido(db, "D", base + timedelta(days=off), total=100)
    dados = svc.montar_recompra(db, vendedor="Joao", cidade=None, uf=None, hoje=base + timedelta(days=200))
    docs = [c["documento"] for c in dados["clientes"]]
    # C (sem padrão) aparece depois de D (tem índice)
    assert docs.index("C") > docs.index("D")
    assert dados["kpis"]["sem_padrao"] >= 1


def test_montar_recompra_filtra_por_vendedor(db):
    _cli(db, "E", vendedor="Maria")
    _pedido(db, "E", date(2026, 1, 1))
    dados = svc.montar_recompra(db, vendedor="Joao", cidade=None, uf=None, hoje=date(2026, 2, 1))
    assert all(c["documento"] != "E" for c in dados["clientes"])


def test_opcoes_recompra_lista_vendedores_e_locais(db):
    _cli(db, "F", vendedor="Joao", uf="PI", municipio="Floriano")
    out = svc.opcoes_recompra(db)
    assert "Joao" in out["vendedores"]
    assert "PI" in out["ufs"]
    assert "Floriano" in out["cidades"]
```

- [ ] **Step 2: Rodar os testes para vê-los falhar**

Run: `docker compose exec -T app sh -c "cd /code && PYTHONPATH=/code pytest tests/test_recompra_service.py -k 'montar or opcoes' -q"`
Expected: FAIL com `AttributeError: module 'recompra_service' has no attribute 'montar_recompra'`.

- [ ] **Step 3: Implementar os helpers de banco em `app/recompra_service.py`**

Acrescentar ao final de `app/recompra_service.py`:

```python
# ----------------------------------------------------------------- helpers de DB

_FILTRO_COMPRA = "ped.orcamento = FALSE AND ped.situacao IS DISTINCT FROM 'Cancelado' AND ped.emissao IS NOT NULL"


def montar_recompra(db: Session, *, vendedor: str | None, cidade: str | None,
                    uf: str | None, hoje) -> dict:
    """Agrega compras efetivas por cliente, classifica pelo ritmo individual e
    devolve {clientes: [...ordenados...], kpis: {...}}."""
    cond = ["(pm.inativo = FALSE OR pm.inativo IS NULL)"]
    params: dict = {}
    if vendedor:
        cond.append("pm.vendedor = :vendedor")
        params["vendedor"] = vendedor
    if cidade:
        cond.append("pm.municipio = :cidade")
        params["cidade"] = cidade
    if uf:
        cond.append("pm.uf = :uf")
        params["uf"] = uf
    where = " AND ".join(cond)

    rows = db.execute(text(f"""
        SELECT
            pm.documento,
            COALESCE(NULLIF(TRIM(pm.nome_fantasia), ''), pm.razao_social, pm.documento) AS nome,
            pm.vendedor, pm.municipio, pm.uf,
            array_agg(ped.emissao ORDER BY ped.emissao)  AS datas,
            COALESCE(SUM(ped.total_liquido), 0)          AS receita_total
        FROM cliente_pedido_mobile pm
        JOIN pedido_mobile_pedido ped ON ped.cliente_documento = pm.documento
        WHERE {where} AND {_FILTRO_COMPRA}
        GROUP BY pm.documento, nome, pm.vendedor, pm.municipio, pm.uf
    """), params).fetchall()

    clientes = []
    for r in rows:
        info = classificar_recompra(list(r.datas), hoje, receita_total=float(r.receita_total or 0))
        info.update({
            "documento": r.documento,
            "nome": r.nome,
            "vendedor": r.vendedor,
            "municipio": r.municipio,
            "uf": r.uf,
        })
        clientes.append(info)

    # Ordena por índice desc; "sem padrão" (índice None) sempre no fim.
    clientes.sort(key=lambda c: (c["indice"] is not None, c["indice"] or 0.0), reverse=True)

    kpis = {
        "em_dia":    sum(1 for c in clientes if c["faixa"] == FAIXA_EM_DIA),
        "atrasando": sum(1 for c in clientes if c["faixa"] == FAIXA_ATRASANDO),
        "atrasado":  sum(1 for c in clientes if c["faixa"] == FAIXA_ATRASADO),
        "sem_padrao": sum(1 for c in clientes if c["faixa"] == FAIXA_SEM_PADRAO),
        "receita_atrasados": round(
            sum(c["ticket_medio"] for c in clientes if c["faixa"] == FAIXA_ATRASADO), 2
        ),
    }
    return {"clientes": clientes, "kpis": kpis}


def opcoes_recompra(db: Session) -> dict:
    """Listas para os selects de filtro (vendedor, UF, cidade) — da base de clientes."""
    vend = db.execute(text("""
        SELECT DISTINCT TRIM(vendedor) AS v FROM cliente_pedido_mobile
        WHERE NULLIF(TRIM(vendedor), '') IS NOT NULL ORDER BY v
    """)).scalars().all()
    ufs = db.execute(text("""
        SELECT DISTINCT TRIM(uf) AS u FROM cliente_pedido_mobile
        WHERE NULLIF(TRIM(uf), '') IS NOT NULL ORDER BY u
    """)).scalars().all()
    cidades = db.execute(text("""
        SELECT DISTINCT TRIM(municipio) AS m FROM cliente_pedido_mobile
        WHERE NULLIF(TRIM(municipio), '') IS NOT NULL ORDER BY m
    """)).scalars().all()
    return {"vendedores": list(vend), "ufs": list(ufs), "cidades": list(cidades)}
```

- [ ] **Step 4: Rodar os testes para vê-los passar**

Run: `docker compose exec -T app sh -c "cd /code && PYTHONPATH=/code pytest tests/test_recompra_service.py -q"`
Expected: PASS (todos do módulo).

- [ ] **Step 5: Commit**

```bash
git add app/recompra_service.py app/tests/test_recompra_service.py
git commit -m "feat: agregador de recompra (query por cliente + kpis + opcoes de filtro)"
```

---

## Task 3: Rota `GET /dashboard/recompra` + cache

**Files:**
- Modify: `app/routers/dashboard.py`

- [ ] **Step 1: Adicionar import e cache em `app/routers/dashboard.py`**

No topo, junto aos outros imports de serviço (após `import analise_service as svc_analise`):

```python
import recompra_service as svc_recompra
```

Após o bloco `_cache_analise` / `_dados_analise_cacheados` (perto da linha 45), acrescentar:

```python
_cache_recompra: dict = {}  # chave -> (ts, dados)


def _dados_recompra_cacheados(db: Session, *, vendedor, cidade, uf, hoje) -> dict:
    chave = "|".join([vendedor or "", cidade or "", uf or "", hoje.isoformat()])
    agora = time.monotonic()
    item = _cache_recompra.get(chave)
    if item and (agora - item[0]) < _CACHE_TTL_SEGUNDOS:
        return item[1]
    dados = svc_recompra.montar_recompra(db, vendedor=vendedor, cidade=cidade, uf=uf, hoje=hoje)
    _cache_recompra[chave] = (agora, dados)
    return dados
```

- [ ] **Step 2: Adicionar a rota** (ao final de `app/routers/dashboard.py`)

```python
@router.get("/dashboard/recompra", response_class=HTMLResponse)
def dashboard_recompra(
    request: Request,
    current_user: dict = Depends(require_login),
    db: Session = Depends(get_db),
):
    hoje = datetime.now(BRT).date()
    q = dict(request.query_params)
    vendedor = q.get("vendedor") or None
    cidade = q.get("cidade") or None
    uf = q.get("uf") or None
    faixa = q.get("faixa") or ""

    dados = _dados_recompra_cacheados(db, vendedor=vendedor, cidade=cidade, uf=uf, hoje=hoje)
    clientes = dados["clientes"]
    if faixa:
        clientes = [c for c in clientes if c["faixa"] == faixa]

    ctx = {
        "request": request,
        "user": current_user,
        "kpis": dados["kpis"],
        "clientes": clientes,
        "opcoes": svc_recompra.opcoes_recompra(db),
        "filtros": {"vendedor": vendedor or "", "cidade": cidade or "", "uf": uf or "", "faixa": faixa},
    }
    template = "partials/recompra_paineis.html" if request.headers.get("HX-Request") else "recompra.html"
    return templates.TemplateResponse(template, ctx)
```

- [ ] **Step 3: Verificar que o app importa sem erro**

Run: `docker compose exec -T app sh -c "cd /code && PYTHONPATH=/code python -c \"import main; print('ok')\""`
Expected: imprime `ok`.

- [ ] **Step 4: Rodar a suíte (sem regressão)**

Run: `docker compose exec -T app sh -c "cd /code && PYTHONPATH=/code pytest -q"`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add app/routers/dashboard.py
git commit -m "feat: rota /dashboard/recompra com cache por recorte"
```

---

## Task 4: Templates (página + parcial) e card no hub

**Files:**
- Create: `app/templates/partials/recompra_paineis.html`
- Create: `app/templates/recompra.html`
- Modify: `app/templates/dashboards.html`

- [ ] **Step 1: Criar o parcial `app/templates/partials/recompra_paineis.html`**

```html
<!-- KPIs -->
<div class="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-5 gap-3 mb-4">
  <div class="bg-[#16241a] border border-[#2a4a2a] rounded-xl p-3">
    <div class="text-green-400 text-2xl font-bold">{{ kpis.em_dia }}</div>
    <div class="text-[#9ca3af] text-xs">🟢 Em dia</div>
  </div>
  <div class="bg-[#241f10] border border-[#4a4424] rounded-xl p-3">
    <div class="text-yellow-400 text-2xl font-bold">{{ kpis.atrasando }}</div>
    <div class="text-[#9ca3af] text-xs">🟡 Atrasando</div>
  </div>
  <div class="bg-[#241010] border border-[#4a2424] rounded-xl p-3">
    <div class="text-red-400 text-2xl font-bold">{{ kpis.atrasado }}</div>
    <div class="text-[#9ca3af] text-xs">🔴 Atrasado</div>
  </div>
  <div class="bg-[#1a1a1a] border border-[#333] rounded-xl p-3">
    <div class="text-[#9ca3af] text-2xl font-bold">{{ kpis.sem_padrao }}</div>
    <div class="text-[#9ca3af] text-xs">⚪ Sem padrão</div>
  </div>
  <div class="bg-[#1f1810] border border-[#4a3a1a] rounded-xl p-3">
    <div class="text-orange-400 text-2xl font-bold">R$ {{ "{:,.0f}".format(kpis.receita_atrasados).replace(",", ".") }}</div>
    <div class="text-[#9ca3af] text-xs">Receita dos atrasados</div>
  </div>
</div>

<!-- Tabela -->
<div class="bg-[#171717] rounded-xl border border-[#2e2e2e] overflow-hidden">
  <table class="w-full text-sm">
    <thead class="text-[#888] border-b border-[#2e2e2e]">
      <tr>
        <th class="text-left px-3 py-2 font-medium">Cliente</th>
        <th class="text-left px-3 py-2 font-medium">Vendedor</th>
        <th class="text-right px-3 py-2 font-medium">Última compra</th>
        <th class="text-right px-3 py-2 font-medium">Ritmo</th>
        <th class="text-right px-3 py-2 font-medium">Dias s/ comprar</th>
        <th class="text-right px-3 py-2 font-medium">Índice</th>
        <th class="text-right px-3 py-2 font-medium">Receita/compra</th>
        <th class="text-center px-3 py-2 font-medium">Faixa</th>
      </tr>
    </thead>
    <tbody>
      {% for c in clientes %}
      <tr class="border-b border-[#1e1e1e] hover:bg-[#1a1a1a]">
        <td class="px-3 py-2 text-[#ddd]">{{ c.nome }}{% if c.municipio %}<span class="text-[#666] text-xs"> · {{ c.municipio }}/{{ c.uf }}</span>{% endif %}</td>
        <td class="px-3 py-2 text-[#aaa]">{{ c.vendedor or '—' }}</td>
        <td class="px-3 py-2 text-right text-[#aaa]">{{ c.ultima_compra.strftime('%d/%m/%y') if c.ultima_compra else '—' }}</td>
        <td class="px-3 py-2 text-right text-[#aaa]">{{ ('~%d d' % c.mediana) if c.mediana is not none else '—' }}</td>
        <td class="px-3 py-2 text-right text-[#aaa]">{{ c.dias_sem_comprar if c.dias_sem_comprar is not none else '—' }}</td>
        <td class="px-3 py-2 text-right font-semibold
            {% if c.faixa == 'atrasado' %}text-red-400{% elif c.faixa == 'atrasando' %}text-yellow-400{% elif c.faixa == 'em_dia' %}text-green-400{% else %}text-[#666]{% endif %}">
          {{ ('%.2f×' % c.indice) if c.indice is not none else '—' }}
        </td>
        <td class="px-3 py-2 text-right text-[#aaa]">R$ {{ "{:,.0f}".format(c.ticket_medio).replace(",", ".") }}</td>
        <td class="px-3 py-2 text-center">
          {% if c.faixa == 'atrasado' %}🔴{% elif c.faixa == 'atrasando' %}🟡{% elif c.faixa == 'em_dia' %}🟢{% else %}⚪{% endif %}
        </td>
      </tr>
      {% else %}
      <tr><td colspan="8" class="px-3 py-8 text-center text-[#555]">Nenhum cliente para este recorte.</td></tr>
      {% endfor %}
    </tbody>
  </table>
</div>
```

- [ ] **Step 2: Criar a página `app/templates/recompra.html`**

```html
{% extends "base.html" %}
{% block title %}SBR Leads — Recompra{% endblock %}

{% block content %}
<div class="space-y-4">
  <div>
    <h1 class="text-xl font-semibold text-white">Recompra</h1>
    <p class="text-sm text-[#888]">Quem está dentro ou fora do próprio ritmo de compra (medição até hoje).</p>
  </div>

  <!-- Filtros: o FORM dispara no change de qualquer select e envia todos os campos juntos -->
  <form hx-get="/dashboard/recompra" hx-target="#recompra-paineis" hx-swap="innerHTML"
        hx-trigger="change" hx-push-url="true"
        class="bg-[#171717] rounded-xl border border-[#2e2e2e] p-4 flex flex-wrap gap-3 items-end">
    <label class="text-xs text-[#888]">Vendedor
      <select name="vendedor"
              class="block mt-1 bg-[#222] border border-[#3a3a3a] rounded-lg px-3 py-2 text-sm text-[#ddd]">
        <option value="">Todos</option>
        {% for v in opcoes.vendedores %}
        <option value="{{ v }}" {{ 'selected' if filtros.vendedor == v else '' }}>{{ v }}</option>
        {% endfor %}
      </select>
    </label>
    <label class="text-xs text-[#888]">UF
      <select name="uf"
              class="block mt-1 bg-[#222] border border-[#3a3a3a] rounded-lg px-3 py-2 text-sm text-[#ddd]">
        <option value="">Todas</option>
        {% for u in opcoes.ufs %}
        <option value="{{ u }}" {{ 'selected' if filtros.uf == u else '' }}>{{ u }}</option>
        {% endfor %}
      </select>
    </label>
    <label class="text-xs text-[#888]">Cidade
      <select name="cidade"
              class="block mt-1 min-w-[160px] bg-[#222] border border-[#3a3a3a] rounded-lg px-3 py-2 text-sm text-[#ddd]">
        <option value="">Todas</option>
        {% for cid in opcoes.cidades %}
        <option value="{{ cid }}" {{ 'selected' if filtros.cidade == cid else '' }}>{{ cid }}</option>
        {% endfor %}
      </select>
    </label>
    <label class="text-xs text-[#888]">Faixa
      <select name="faixa"
              class="block mt-1 bg-[#222] border border-[#3a3a3a] rounded-lg px-3 py-2 text-sm text-[#ddd]">
        <option value="" {{ 'selected' if filtros.faixa == '' else '' }}>Todas</option>
        <option value="atrasado" {{ 'selected' if filtros.faixa == 'atrasado' else '' }}>🔴 Atrasado</option>
        <option value="atrasando" {{ 'selected' if filtros.faixa == 'atrasando' else '' }}>🟡 Atrasando</option>
        <option value="em_dia" {{ 'selected' if filtros.faixa == 'em_dia' else '' }}>🟢 Em dia</option>
        <option value="sem_padrao" {{ 'selected' if filtros.faixa == 'sem_padrao' else '' }}>⚪ Sem padrão</option>
      </select>
    </label>
  </form>

  <div id="recompra-paineis">
    {% include "partials/recompra_paineis.html" %}
  </div>
</div>
{% endblock %}
```

> Nota: o **form** tem `hx-trigger="change"`, então trocar qualquer `<select>` filho dispara um único GET com **todos** os campos do form (vendedor+uf+cidade+faixa) na querystring. O `hx-target="#recompra-paineis"` troca só os KPIs+tabela; `hx-push-url="true"` mantém o recorte na URL.

- [ ] **Step 3: Adicionar o card no hub `app/templates/dashboards.html`**

Dentro do `<div class="grid ...">`, após o card da Análise de Vendas (o `</a>` que fecha o card `/dashboard/analise`), inserir:

```html
    <a href="/dashboard/recompra" class="bg-[#171717] border border-[#2e2e2e] rounded-xl p-5 hover:border-orange-500 hover:-translate-y-0.5 transition-all group">
      <div class="text-2xl">🔁</div>
      <div class="text-white font-semibold mt-2 group-hover:text-orange-500">Recompra</div>
      <div class="text-[#888] text-xs mt-1 leading-relaxed">Quem está fora do próprio ritmo de compra — em dia, atrasando ou atrasado, por cliente.</div>
      <span class="inline-block mt-3 text-[9px] px-2 py-0.5 rounded-full bg-green-500/12 text-green-400">Disponível</span>
    </a>
```

- [ ] **Step 4: Verificar compilação dos templates + import do app + suíte**

Run:
```
docker compose exec -T app sh -c "cd /code && PYTHONPATH=/code python -c \"from fastapi.templating import Jinja2Templates; e=Jinja2Templates(directory='templates').env; e.get_template('recompra.html'); e.get_template('partials/recompra_paineis.html'); e.get_template('dashboards.html'); print('templates ok')\""
docker compose exec -T app sh -c "cd /code && PYTHONPATH=/code pytest -q"
```
Expected: `templates ok` e suíte verde.

- [ ] **Step 5: Verificação manual no navegador**

Subir o app (se não estiver no ar) e, logado (admin@sbr.local), abrir `http://localhost:8000/dashboards` → card **Recompra** → `http://localhost:8000/dashboard/recompra`. Verificar:
1. KPIs aparecem com contagens; tabela lista clientes ordenados pelo índice (atrasados no topo, "sem padrão" no fim).
2. Trocar Vendedor / UF / Cidade / Faixa recarrega só os painéis (HTMX) e a URL atualiza.
3. Faixa "Atrasado" mostra só os 🔴, mas os KPIs continuam com a contagem total.

Expected: tudo funciona, sem erro no console.

- [ ] **Step 6: Commit**

```bash
git add app/templates/recompra.html app/templates/partials/recompra_paineis.html app/templates/dashboards.html
git commit -m "feat: tela do dashboard de Recompra (filtros, KPIs, tabela) e card no hub"
```

---

## Verificação final

- [ ] `docker compose exec -T app sh -c "cd /code && PYTHONPATH=/code pytest -q"` — suíte inteira verde.
- [ ] Fluxo manual da Task 4 funciona no navegador.
- [ ] Atualizar `docs/CLAUDE.md` (estado do projeto) e a memória marcando o dashboard de Recompra como implementado, e que o **Cross-sell** é a próxima etapa.

## Notas e riscos

- **Percentil p90 com poucos intervalos:** clientes com exatamente 3 compras têm 2 intervalos; o p90 fica próximo do maior dos dois (esperado). A faixa 🟡 pode ser estreita nesses casos — é o comportamento desejado (pouco histórico = menos tolerância).
- **Cidade como texto livre:** `cliente_pedido_mobile.municipio` vem do Pedido Mobile (nome, não código IBGE). O filtro casa por nome exato; o select é populado com os distintos da base, então não há digitação livre.
- **`array_agg` retorna `list[date]`:** o psycopg2 devolve a coluna agregada como lista de `date` do Python — consumida direto por `classificar_recompra`.
- **Cache:** medição é "até hoje", então a chave inclui `hoje.isoformat()`; vira o dia, a chave muda e o cache recalcula naturalmente.
